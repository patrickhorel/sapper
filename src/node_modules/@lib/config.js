export default {
  mongo: {
    // you can override this with environment variable MONGO_URI
    // uri: process.env.MONGO_URI || "mongodb://localhost",
    uri : "mongodb+srv://admin:Mongo1024@cluster0.kzrdh.mongodb.net/<dbname>?retryWrites=true&w=majority",
    options: {
      useUnifiedTopology: true
    },
    // you can override this with environment variable MONGO_DB
    db: process.env.MONGO_DB || "membres",
  }
}